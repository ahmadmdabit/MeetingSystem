import { MeetingStatusPipe } from './meeting-status-pipe';

describe('MeetingStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new MeetingStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
